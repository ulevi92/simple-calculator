import { IonCol, IonRow } from "@ionic/react";

const PrimaryScreen = () => {
  return (
    <IonRow style={{ height: "52vh" }}>
      <IonCol></IonCol>
    </IonRow>
  );
};

export default PrimaryScreen;
